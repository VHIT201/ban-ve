// @ts-nocheck

export type GetApiAdminStatsParams = {
/**
 * Mốc thời gian bắt đầu (YYYY-MM-DD). Nếu không nhập sẽ mặc định là 7 ngày trước.
 */
startDate?: string;
/**
 * Mốc thời gian kết thúc (YYYY-MM-DD). Nếu không nhập sẽ mặc định là hôm nay.
 */
endDate?: string;
};
