// @ts-nocheck

export type GetApiFileIdDownloadParams = {
/**
 * Email đã dùng để thanh toán (Bắt buộc đối với khách vãng lai)
 */
email?: string;
/**
 * ID của file cụ thể muốn tải (nếu để trống sẽ tải tất cả file trong đơn hàng)
 */
fileId?: string;
};
