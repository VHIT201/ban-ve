// @ts-nocheck

export interface StatItem {
  /** Tổng số hiện tại */
  total?: number;
  /** Phần trăm tăng trưởng so với kỳ trước */
  growth?: number;
  /** Số lượng mới trong kỳ hiện tại */
  new?: number;
}
