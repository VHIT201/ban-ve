import { Card, CardContent } from "@/components/ui/card";

const CollaboratorSendRequest = () => {
  return (
    <Card>
      <CardContent>Collaborator Send Request</CardContent>
    </Card>
  );
};

export default CollaboratorSendRequest;
